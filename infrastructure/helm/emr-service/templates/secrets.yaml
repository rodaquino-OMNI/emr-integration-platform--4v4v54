apiVersion: v1
kind: Secret
metadata:
  name: {{ include "emr-service.fullname" . }}
  labels:
    {{- include "emr-service.labels" . | nindent 4 }}
type: Opaque
data:
  # References to AWS Secrets Manager via External Secrets Operator
  # This is a placeholder - actual secrets are managed by External Secrets Operator
  # See: https://external-secrets.io/
stringData:
  # Placeholder for external secret references
  DATABASE_URL: "postgresql://placeholder"
  REDIS_URL: "redis://placeholder"
  JWT_SECRET: "placeholder"
